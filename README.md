# StorageCleanerMac Updates

This public repository hosts the signed Sparkle appcast and release binaries for
`存储清理助手` (`com.local.StorageCleanerMac`). The application source code is
maintained separately in a private repository.

Update archives are verified with Sparkle EdDSA signatures before extraction.
The current builds are Apple Development signed and are not Apple notarized.
